#include <bits/stdc++.h>

using namespace std;

int main()
{
   ios::sync_with_stdio(false);
   cin.tie(0);
   int a,b,c;
   cin>>a>>b>>c;
   if((a*a) + (b*b) >= (c*c))
   {
       cout<<"No";
   }
   else
   {
       cout<<"Yes";
   }
}